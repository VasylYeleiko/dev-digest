# reviewer-core/specs

Feature/RFC specs for this package — written before or alongside
implementation, one file per feature.

Not to be confused with the `specs` prompt slot `assemblePrompt()` can
accept (a later-lesson input fed *into* review prompts, see
[`../README.md`](../README.md)) — this folder is contributor-facing
documentation, not runtime input.

Nothing here yet.
