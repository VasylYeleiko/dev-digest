# client/docs

Deep-dive notes that don't belong in [`../CLAUDE.md`](../CLAUDE.md) (the
map) or [`../README.md`](../README.md) (the UI route map) — component
design rationale, non-obvious UI state decisions.

- [ui-architecture.md](ui-architecture.md) — the Server/Client Component
  split, the `_components/` convention, the data-hooks rule

Add one file per topic rather than a single growing doc.
