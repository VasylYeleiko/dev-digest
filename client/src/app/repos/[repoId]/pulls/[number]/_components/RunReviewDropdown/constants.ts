/** Constants for RunReviewDropdown. */

/** Dropdown menu width (px). Widened from the original 250 to fit an agent
 *  row's checkbox + name + model hint stacked without wrapping. */
export const DROPDOWN_WIDTH = 280;
