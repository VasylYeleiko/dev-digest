import { SkillType } from "@devdigest/shared";

/** Selectable skill types in the Config tab — straight from the contract enum. */
export const SKILL_TYPE_VALUES: readonly SkillType[] = SkillType.options;
