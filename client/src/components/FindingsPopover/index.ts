/* FindingsPopover — severity icons + a read-only hover/focus/click preview
   popover ("N FINDINGS IN THIS RUN"). Shared by the PR list's FINDINGS cell
   and the PR detail TIMELINE's per-run row. No Accept/Dismiss — that stays on
   the PR detail page's ReviewRunAccordion. */
export { FindingsPopover, type FindingsCounts, type FindingsPreviewItem } from "./FindingsPopover";
