/** Constants for FindingsPopover. */

/** Panel width (px) — must match `styles.ts`'s `panel.width`; used to clamp
 *  the panel's `left` so it never overflows the viewport's right edge. */
export const PANEL_WIDTH = 360;

/** Minimum gap (px) kept between the panel and the viewport's right edge. */
export const VIEWPORT_MARGIN = 16;
