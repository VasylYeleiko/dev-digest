/**
 * widgets — public API (ring 1): types, ports, constants only. No Container,
 * no classes — wiring lives in compose.ts.
 */
export type { WidgetEntity } from './types.js';
export type { WidgetStore } from './ports.js';
