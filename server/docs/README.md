# server/docs

Deep-dive notes that don't belong in [`../CLAUDE.md`](../CLAUDE.md) (the
map) or [`../README.md`](../README.md) (the architecture overview) —
design rationale, non-obvious pipeline internals, decisions worth more than
a bullet.

- [architecture.md](architecture.md) — the DI container, adapter ports, and
  the test-override seam

Add one file per topic (e.g. `repo-intel-pipeline.md`) rather than a single
growing doc.
