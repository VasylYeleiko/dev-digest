# server/specs

Feature/RFC specs for this package — written before or alongside
implementation, one file per feature (e.g. `0001-blast-radius.md`).

Not to be confused with the `specs` prompt slot `reviewer-core/prompt.ts`
can accept (a later-lesson input fed *into* review prompts) — this folder is
contributor-facing documentation, not runtime input.

Nothing here yet.
